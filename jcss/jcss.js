var JCSS = (function($, $H){	return {		version: "1.0",		addStylesheet: function(css){			if(css instanceof Array) css = css.join("\n");			var ssheet = $($H.tag("style", [{type:"text/css"}, css])); 			$("head").append(ssheet);			return ssheet;		},		addRule: function(ssheet, css){ssheet = $(ssheet);			// Такой подход не работает в IE - браузер не позволяет добавлять правила в уже существующую таблицу стилей.			var css1 = ssheet.html();			// alert(css1+"\n"+css);			ssheet.html(css1+"\n"+css);			//ssheet.innerHTML = css1+"\n"+css;		}	};})(jQuery, Html);